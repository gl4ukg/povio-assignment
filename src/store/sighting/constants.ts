const scope = 'sighting';

export const SET_LOADING = `${scope}/SET_LOADING`

export const LOAD_USER_SIGHTINGS = `${scope}/LOAD_USER_SIGHTINGS`
export const SET_USER_SIGHTINGS = `${scope}/SET_USER_SIGHTINGS`