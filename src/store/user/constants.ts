const scope = 'user'

export const SET_LOADING = `${scope}/SET_LOADING`;
export const LOAD_SIGNUP = `${scope}/LOAD_SIGNUP`;
export const LOAD_LOGIN = `${scope}/LOAD_LOGIN`;
export const IS_LOGIN = `${scope}/IS_LOGIN`;

export const LOAD_USER = `${scope}/LOAD_USER`
export const SET_USER = `${scope}/SET_USER`